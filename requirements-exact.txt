# EXACT — Environment 1: foundation model
# For EXACT_Pretrain, EXACT_ClassFinetune, EXACT-Seg
#
# torch is NOT listed here — it comes from the base image
# pytorch/pytorch:2.4.1-cuda12.1-cudnn9-devel, or install manually:
#   pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# mamba-ssm must be installed SEPARATELY and LAST (needs a compiler and
# an already-present torch):
#   pip install -r requirements-exact.txt
#   pip install --no-build-isolation mamba-ssm==2.2.4

# --- numerical / IO ---
numpy==2.1.2
scipy==1.14.1
pandas==2.2.3
h5py==3.12.1
joblib==1.4.2
PyYAML==6.0.2
requests==2.32.3
psutil==6.1.0
tqdm==4.66.5

# --- medical imaging ---
nibabel==5.3.2
SimpleITK==2.4.0
MedPy==0.5.2
torchio==0.20.1
monai==1.3.0

# --- vision / model ---
timm==1.0.11
einops==0.8.0
opencv-python==4.10.0.84
pillow==11.0.0
scikit-image==0.24.0
scikit-learn==1.5.2
thop==0.1.1.post2209072238

# --- logging / plotting ---
swanlab==0.6.5
wandb==0.18.6
tensorboardX==2.6.2.2
matplotlib==3.9.2
seaborn==0.13.2
