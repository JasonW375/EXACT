# EXACT — Environment 2: EXACT-CHAT (report generation)
# For EXACT-CHAT only. Keep separate from requirements-exact.txt:
# mamba-ssm and deepspeed/peft have conflicting build requirements.
#
# torch is NOT listed here — it comes from the base image
# pytorch/pytorch:2.4.1-cuda12.1-cudnn9-devel, or install manually:
#   pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# Then:
#   pip install -r requirements-exactchat.txt
#   cd EXACT-CHAT && pip install -e .

# --- LLM / training ---
transformers==4.47.0
tokenizers==0.21.0
accelerate==1.6.0
peft==0.15.2
deepspeed==0.16.7
bitsandbytes==0.43.0
huggingface-hub==0.25.2
safetensors==0.4.5
sentencepiece==0.2.0
einops==0.8.0
einops-exts==0.0.4

# --- numerical / IO ---
numpy==2.1.2
scipy==1.14.1
pandas==2.2.3
joblib==1.4.2
PyYAML==6.0.2
requests==2.32.3
psutil==6.1.0
filelock==3.16.1
typing-extensions==4.12.2
sympy==1.13.3
regex==2024.11.6
tqdm==4.66.5

# --- medical imaging ---
nibabel==5.3.2
SimpleITK==2.4.0
torchio==0.20.1
monai==1.3.0
opencv-python-headless==4.10.0.84
pillow==11.0.0
scikit-learn==1.5.2

# --- evaluation metrics ---
pycocoevalcap==1.2
nltk==3.9.1
matplotlib==3.9.2

# --- OPTIONAL ---------------------------------------------------------------
# Not needed for training, inference, or the reported evaluations. Install
# only if you use the corresponding component.
#
# Gradio demo server (llava/serve/gradio_*.py, model_worker.py):
#   gradio, fastapi, uvicorn, markdown, fire
#
# SGLang serving backend (llava/serve/sglang_worker.py):
#   sglang
#
# GPT-4.1 refinement / report translation (utils/translate_ct_reports_mianyang.py):
#   openai
#
# Only if you re-enable the CLIP vision tower in ct_clip.py (unused by default —
# visual features are pre-computed offline by the frozen Y-Mamba backbone):
#   git+https://github.com/bfshi/scaling_on_scales.git   # s2wrapper
#
# Attention monkey-patches (llava/train/llama_{flash,xformers}_attn_monkey_patch.py,
# both guarded by try/except and not imported by the default training path):
#   flash-attn, xformers
